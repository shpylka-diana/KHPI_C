#define R1 12       // опір першого резистора
#define R2 23       // опір другого резистора
#define R3 9        // опір третього резистора

int main()
{
    float r = 1 / ((1 / R1) + (1 / R2) + (1 / R3)); //загальний опір трьох резисторів
    
    return 0;
}
